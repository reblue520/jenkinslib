#!groovy

pipeline{
    agent { node { label "main"}}
    
    
    stages{

        stage("build"){
            steps{
                script{
                   mvnHome = tools "M2"
		   sh {${mvnHome}/bin/mvn -v
		}
	    }
	}
    }
}
}
