#!groovy

pipeline {
  agent { node { label: "main"} }
  
  stages {
      stage("build"){
        steps{
          script{
              mvnhome = tool "M2"
              sh "{mvnHome/bin/mvn -v}"
          }
        }
    }
  }
}
