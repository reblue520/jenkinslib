#!groovy

pipeline{
    agent { node { label "build"}}
    
    
    stages{

        stage("CheckOut"){
            steps{
                script{
                   mvnHome = tools "M2"
		   sh {${mvnHome}/bin/mvn -v
		}
	    }
	}
    }
}
